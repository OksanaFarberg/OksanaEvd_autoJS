Installation
To set up the project, run:
npm install

To execute the test suite:
npm run test

Чтобы посмотреть отчет - надо расскомментировать в джест конфиге
"openReport": !process.env.CI
тогда после прохождения тестов 

To use ci for install dependencies and run tests:
npm ci

 